<div class="content">
  <div class="container-fluid">
    <h4 class="page-title">
      <% if (mode === "Create") { %> Create New Product Category <% } %> 
      <% if (mode === "Update") { %> Update Product Category <% } %>
    </h4>
    <div class="card">
      <div class="card-body">
        <% if (mode === "Create") { %>
        <form action="/categories/create" method="post">
        <% } %> 
        <% if (mode === "Update") { %>
        <form action="/categories/update" method="post">
          <div class="form-group hide">
            <input
              name="id"
              type="text"
              value="<%=category?.id%>"
              id="id"
            />
          </div>
        <% } %>
          <div class="form-group">
            <label for="code">ID Type Product</label>
            <input
              name="code"
              type="text"
              value="<%=category?.code%>"
              class="form-control"
              id="code"
              placeholder="Enter ID product"
            />
          </div>
          <div class="form-group">
            <label for="name">Name Type Product</label>
            <input
              name="name"
              type="text"
              value="<%=category?.name%>"
              class="form-control"
              id="name"
              placeholder="Enter name product"
            />
          </div>
          <div class="form-group">
            <label for="image">Link Image</label>
            <input
              name="image"
              type="text"
              value="<%=category?.image%>"
              class="form-control"
              id="image"
              placeholder="Enter link image"
            />
          </div>
          <div class="card-action">
            <% if (mode === "Create") { %>
            <button class="btn btn-success">Create</button>
            <% } %> 
            <% if (mode === "Update") { %>
            <button class="btn btn-success">Update</button>
            <% } %>
            <a class="btn btn-primary" href="/categories">Return</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
